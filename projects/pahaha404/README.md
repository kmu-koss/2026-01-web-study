# pahaha404 제출 폴더

이 폴더는 `pahaha404` 브랜치에서 관리하는 제출 예시입니다.
`main` 브랜치에는 이 폴더를 두지 않고, `pahaha404` 브랜치에서만 관리합니다.

권장 사용 방식:

```text
branch: pahaha404

projects/
  pahaha404/
    week1/
    week2/
    final/
```

현재 예시로 `week1/` 폴더가 준비되어 있습니다.
